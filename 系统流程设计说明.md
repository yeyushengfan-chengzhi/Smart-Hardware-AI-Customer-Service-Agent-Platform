# Smart Hardware AI Customer Service Agent Platform 系统流程设计

## 1. 系统整体流程

本系统采用 FastAPI 提供后端服务，结合 SupervisorAgent 实现请求分类与路由。当前实现中，SupervisorAgent 对用户问题进行规则化意图识别，并将请求分派至 KnowledgeAgent、DiagnosisAgent 或 ToolAgent。各 Agent 独立完成相应任务后，由 FastAPI 接口返回结构化结果，同时写入 Agent Trace 记录。

对于无法识别的普通问题，系统当前返回 GeneralAgent 路由及人工转接建议；对于售后类问题，路由结果为 AfterSalesAgent，但该 Agent 的具体执行逻辑未在当前代码中实现。

```text
User Query
    |
    v
FastAPI Agent API
    |
    v
SupervisorAgent
    |
    +----------------------+-----------------------+----------------------+
    |                      |                       |                      |
    v                      v                       v                      v
KnowledgeAgent        DiagnosisAgent           ToolAgent          General / AfterSales
    |                      |                       |                      |
    v                      v                       v                      v
RAG + LLM          RAG-grounded Steps      Local Tool Calling   Handoff Suggestion
    |                      |                       |                      |
    +----------------------+-----------------------+----------------------+
                                   |
                                   v
                         FastAPI Structured Response
                                   |
                                   v
                              Agent Trace Storage
                                   |
                                   v
                                Return to User
```

系统中的关系型业务数据、知识文档元数据、文本切片、会话数据、评测数据及 Trace 数据由 MySQL 持久化保存；向量化后的知识片段存储于 Chroma 向量数据库。Vue3 Admin Console 用于知识库、Agent 配置、运行追踪和评测结果的可视化管理。

## 2. 用户请求处理流程

用户通过前端管理控制台或 API 向 FastAPI 服务提交问题。FastAPI 调用 SupervisorAgent 对问题进行意图识别，并输出意图类别、设备类型、故障类型及目标路由。

当前已实现的主要路由逻辑包括：

- 产品知识查询：`product_info`，路由至 KnowledgeAgent。
- 故障诊断：`fault_diagnosis`，路由至 DiagnosisAgent。
- 硬件兼容性查询：`compatibility_check`，路由至 ToolAgent。
- 硬件规格工具查询：`tool_call`，路由至 ToolAgent。
- 普通或未识别问题：`unknown`，返回 GeneralAgent 路由及转人工建议。
- 售后问题：`after_sales`，返回 AfterSalesAgent 路由；当前未实现独立执行 Agent。

```text
User Query
    |
    v
FastAPI Request Endpoint
    |
    v
SupervisorAgent
    |
    +--> product_info ----------> KnowledgeAgent
    |
    +--> fault_diagnosis -------> DiagnosisAgent
    |
    +--> compatibility_check ---> ToolAgent
    |
    +--> tool_call -------------> ToolAgent
    |
    +--> unknown ---------------> GeneralAgent Route / Handoff Suggestion
    |
    +--> after_sales -----------> AfterSalesAgent Route
```

SupervisorAgent 当前采用基于关键词、设备名称、故障表达及硬件型号模式的规则进行识别。例如，CPU 型号、主板型号与“兼容”“搭配”等表达共同出现时，系统将其识别为兼容性查询。

## 3. KnowledgeAgent RAG 流程

KnowledgeAgent 用于回答产品规格、接口、功能、兼容性说明及使用手册相关问题。其回答建立在企业知识库检索结果之上，以减少回答脱离产品文档的风险。

知识文档处理流程如下：

```text
PDF Knowledge Document
    |
    v
Document Upload and Metadata Storage
    |
    v
PDF Parsing
    |
    v
Text Chunking
    |
    v
Chunk Metadata Stored in MySQL
    |
    v
Embedding Vectorization
    |
    v
Chroma Vector Database
```

在线问答流程如下：

```text
User Product Question
    |
    v
SupervisorAgent: product_info
    |
    v
KnowledgeAgent
    |
    v
Query Rewrite / Concept Expansion
    |
    v
RAG Retrieval
    |
    v
Chroma Vector Search
    |
    v
Semantic and Keyword Hybrid Ranking
    |
    v
Relevant Chunks
    |
    v
LLM Grounded Answer Generation
    |
    v
Answer with Sources
```

KnowledgeAgent 在检索前对原始查询进行概念扩展。当前实现覆盖内存、CPU、PCIe、M.2、显示输出及供电等硬件概念，将用户常用表达扩展为说明书中可能出现的中英文术语，以增强召回效果。

RAG 服务从 Chroma 获取候选文本片段后，结合语义相似度与关键词覆盖度进行加权排序，并执行阈值过滤和重复片段去除。仅状态为 `active` 且 Embedding 状态为 `completed` 的知识文档可参与正式 RAG 检索。

最终回答通过 LLM 基于检索到的上下文生成，并返回 Sources 引用信息。每条来源包含文件名、页码和章节标题，便于用户追溯答案依据。

## 4. DiagnosisAgent 故障诊断流程

DiagnosisAgent 面向硬件故障、设备无法识别、无法启动、无显示、过热和安装异常等问题生成结构化排查建议。

```text
User Fault Description
    |
    v
SupervisorAgent: fault_diagnosis
    |
    v
DiagnosisAgent
    |
    v
Device and Fault Type Recognition
    |
    v
Diagnostic Plan Selection
    |
    v
RAG Retrieval for Each Diagnostic Step
    |
    v
Structured Troubleshooting Steps
    |
    v
Return to User
```

DiagnosisAgent 首先结合 SupervisorAgent 输出的设备类型和故障类型进行诊断规划；若路由结果缺失，则在 Agent 内部再次依据问题文本匹配设备与故障特征。

当前实现的诊断流程主要包含以下层次：

```text
Device Identification
    |
    v
Fault Type Determination
    |
    v
Diagnostic Step Template Selection
    |
    v
Knowledge-base Evidence Retrieval
    |
    v
Structured Step Output
```

对于显卡和内存相关问题，系统提供针对性的排查步骤，例如 PCIe 插槽与供电检查、GPU 检测状态检查、DRAM 指示灯检查及 DIMM 重装检查。对于无显示、启动失败、过热、硬件错误和安装异常等通用问题，系统根据预定义诊断模板生成排查顺序。

每个排查步骤均包含操作建议、操作原因及对应的知识库来源，实现诊断建议与文档证据的关联。

## 5. ToolAgent 工具调用流程

ToolAgent 面向需要确定性结果的硬件规格与兼容性问题。当前实现的工具均基于本地维护的硬件规格数据文件执行，不依赖模型直接推测硬件参数。

以“13700K 可以搭配 B850 主板吗”为例，系统流程如下：

```text
User Query
    |
    v
SupervisorAgent: compatibility_check
    |
    v
ToolAgent
    |
    v
Extract CPU and Motherboard Entities
    |
    v
compatibility_check_tool
    |
    v
Local Hardware Specification Data
    |
    v
Structured Compatibility Result
    |
    v
ToolAgent Answer Generation
    |
    v
Return to User
```

当前已实现的工具包括：

- `compatibility_check_tool`：根据本地主板规格数据、CPU 支持系列及示例型号，返回兼容、未知或需人工确认的结果。
- `hardware_spec_tool`：根据本地硬件规格数据返回已收录产品的插槽、CPU 支持范围、内存类型等结构化参数。

ToolAgent 会从问题中提取 CPU 型号与主板型号，并根据问题中的“兼容”“搭配”“规格”等表达选择相应工具。对于本地数据未收录、无法确认的情况，工具返回 `unknown` 状态，并给出查询官方兼容列表、BIOS 版本或转人工处理的建议。

工具调用通过确定性数据查询约束回答范围，可降低 LLM 对硬件兼容性和固定规格进行自由生成时产生幻觉的风险。

## 6. Agent Trace 流程

系统为 Agent 请求建立独立的 Trace 记录，用于调试、审计和可观测性分析。Trace 保存采用独立数据库会话，即使追踪写入失败，也不会中断面向用户的主请求。

```text
User Request
    |
    v
Route Record
    |
    v
Agent Execution Record
    |
    +--> Knowledge Sources
    |
    +--> Tool Input and Tool Result
    |
    +--> Structured Agent Response
    |
    v
Latency Record
    |
    v
Final Answer / Error Status
    |
    v
MySQL Agent Trace Storage
```

当前 Trace 记录内容包括：

- Trace ID、原始用户问题及创建时间；
- 路由结果、意图类型、设备类型和故障类型；
- 实际执行的 Agent 名称；
- 最终回答与知识来源；
- 工具名称、工具输入和工具执行结果；
- 路由响应与完整 Agent 响应；
- 路由耗时、Agent 执行耗时和总耗时；
- 成功或失败状态、错误信息；
- 是否建议转人工及其原因。

Vue3 Admin Console 的 Agent Trace 页面可对 Trace 按路由、Agent 名称和运行状态进行查询，用于定位路由错误、工具执行异常、知识库召回不足及响应性能问题。

## 7. Evaluation 流程

系统实现了面向 Agent 路由、工具调用和回答内容的批量评测机制。评测案例及评测结果存储在 MySQL 中，可用于回归测试和持续优化。

```text
Evaluation Case Set
    |
    v
Batch Agent Invocation
    |
    v
Supervisor Routing and Agent Execution
    |
    v
Agent Trace ID
    |
    v
Result Comparison
    |
    +--> Expected Route vs. Actual Route
    |
    +--> Expected Agent vs. Actual Agent
    |
    +--> Expected Tool vs. Actual Tool
    |
    +--> Expected Keywords vs. Agent Output
    |
    v
Score Calculation
    |
    v
Pass / Fail and Pass Rate
```

每个测试案例包含问题文本、预期路由、预期 Agent、预期关键词及可选的预期工具名称。批量执行时，系统根据实际路由调用对应 Agent，并关联该次执行产生的 Trace ID。

评测计算主要考虑以下指标：

- Route Match：实际路由是否与预期路由一致；
- Agent Match：实际执行 Agent 是否与预期 Agent 一致；
- Tool Match：工具调用名称是否符合预期；
- Keyword Score：回答及 Agent 响应中是否覆盖预期关键词；
- Final Score：由路由、Agent、工具和关键词匹配结果加权计算；
- Pass Rate：通过案例数占总案例数的比例。

当前系统以 80 分作为测试通过阈值，并记录每条失败案例的错误原因。

## 8. Knowledge Center 流程

Knowledge Center 是企业知识资产的管理入口，由 Vue3 Admin Console 与 FastAPI 知识库接口共同构成。该模块支持文档上传、文档状态管理、解析、向量化、切片查看和检索测试。

```text
Upload Document
    |
    v
Document Metadata Management
    |
    v
PDF Parsing
    |
    v
Chunk Storage and Viewing
    |
    v
Embedding Status Management
    |
    v
Chroma Vector Storage
    |
    v
Retrieval Test
    |
    v
KnowledgeAgent / DiagnosisAgent Retrieval
```

知识文档在上传后保存文件名、原始文件名、产品名称、产品类别、版本、文件哈希、文档状态、切片数量和向量化状态等元数据。系统通过文件哈希识别重复文档，避免重复上传。

文档处理状态包括：

- 文档状态：如 `active`；
- Embedding 状态：`pending`、`processing`、`completed`、`failed`；
- 切片数量：记录文档解析后生成的文本 Chunk 数量。

管理员可查看指定文档的 Chunk 内容、页码和章节标题，并可使用检索测试接口验证向量化结果。正式 RAG 调用仅检索处于 `active` 且 `completed` 状态的文档，从而保证知识资产的可控性。

## 9. 系统总体闭环

系统形成了“知识管理—智能路由—Agent 执行—结果追踪—评测优化”的闭环架构。知识中心持续维护可检索的企业文档；SupervisorAgent 将用户问题分配至适当处理单元；KnowledgeAgent 和 DiagnosisAgent 使用 RAG 获取文档依据；ToolAgent 通过结构化工具处理确定性硬件问题；Trace 和 Evaluation 则为系统调试与质量优化提供数据基础。

```text
Knowledge Document Upload
    |
    v
Parsing, Chunking and Embedding
    |
    v
Chroma Vector Database + MySQL Metadata
    |
    v
User Query
    |
    v
FastAPI
    |
    v
SupervisorAgent
    |
    +------------------------+-----------------------+
    |                        |                       |
    v                        v                       v
KnowledgeAgent         DiagnosisAgent           ToolAgent
    |                        |                       |
    v                        v                       v
RAG + LLM            RAG-grounded Steps     Local Tool Calling
    |                        |                       |
    +------------------------+-----------------------+
                             |
                             v
                    Structured User Response
                             |
                             v
                      Agent Trace Recording
                             |
                             v
                    Evaluation Case Comparison
                             |
                             v
                  Pass Rate and Error Analysis
                             |
                             v
                  Knowledge and Rule Optimization
```
